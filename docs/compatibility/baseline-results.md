# Baseline results

The authoritative disposable-checkout command record is [TypeScript baseline results](typescript-baseline-results.md).
The pinned model catalog was hydrated only in that disposable checkout.
The complete offline build and deterministic Pi suite passed, including telemetry, AI, agent, protocol, client, server, SQLite, TUI, coding-agent, evaluation, script, and browser-smoke checks.
Credential-gated and external-service tests remain explicit optional skips.
